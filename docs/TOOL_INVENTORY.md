# Tool Inventory

This file is generated by `npm run inspect-tools:write`. Do not edit it manually.

Source: runtime MCP tools/list plus src/tools/*/register.ts.

## Verified Counts

- Tools in full admin/write mode: **55**
- Resources in full admin/write mode: **6**
- Prompts in full admin/write mode: **5**

## Profile Counts

| Profile | Read-only mode | Write-enabled mode |
| --- | ---: | ---: |
| `read` | 18 | 18 |
| `finance` | 28 | 35 |
| `webhooks` | 8 | 13 |
| `support` | 5 | 5 |
| `admin` | 43 | 55 |

## Tools

| Tool | Category | Required permission | Classification | Confirmation required | Available read-only | Description |
| --- | --- | --- | --- | --- | --- | --- |
| `sezzle_get_audit_event` | Audit | `admin` | read | No | Yes | Get one redacted audit event by audit ID. |
| `sezzle_list_audit_events` | Audit | `admin` | read | No | Yes | List redacted mutation and ingestion audit metadata. Payloads and secrets are not stored. |
| `sezzle_audit_auth_capture_flow` | Integration Doctor | `read` | read | No | Yes | Audit supplied authorization, capture, refund, reference, and currency state for deterministic flow defects. |
| `sezzle_detect_duplicate_refunds` | Integration Doctor | `read` | read | No | Yes | Detect duplicate refund IDs or repeated amount/time fingerprints in supplied order evidence. |
| `sezzle_detect_stuck_authorizations` | Integration Doctor | `read` | read | No | Yes | Find approved uncaptured authorizations that have not progressed within a threshold. |
| `sezzle_detect_uncaptured_orders` | Integration Doctor | `read` | read | No | Yes | Find approved orders whose captured amount remains below authorization. |
| `sezzle_diagnose_integration` | Integration Doctor | `read` | read | No | Yes | Run deterministic Integration Doctor checks over supplied configuration, order, webhook, and reconciliation evidence. |
| `sezzle_generate_go_live_checklist` | Integration Doctor | `read` | read | No | Yes | Generate a deterministic production-readiness checklist and blocking findings from supplied evidence. |
| `sezzle_test_webhook_configuration` | Integration Doctor | `read` | read | No | Yes | Evaluate webhook subscriptions, invalid signatures, missing events, and out-of-order evidence without sending a test event. |
| `sezzle_validate_redirect_urls` | Integration Doctor | `read` | read | No | Yes | Validate cancel and complete redirects against HTTPS and supplied merchant hosts. |
| `sezzle_validate_session_payload` | Integration Doctor | `read` | read | No | Yes | Validate a proposed Sezzle session payload deterministically without sending customer or order data to Sezzle. |
| `sezzle_authenticate_merchant` | Merchant operations | `read` | read | No | Yes | Acquire or refresh a merchant bearer token from configured environment credentials. Secrets are never returned. |
| `sezzle_cancel_active_checkout` | Merchant operations | `finance` | write | Yes | No | Two-step irreversible deletion of an active incomplete checkout. Call first with confirm=false. |
| `sezzle_capture_order` | Merchant operations | `finance` | write | Yes | No | Execute exactly the matching capture preview. Requires preview_id and literal confirm=true. |
| `sezzle_create_payment_session` | Merchant operations | `finance` | write | Yes | No | Two-step session creation. Call with confirm=false for a safe preview, then repeat unchanged with preview_id and confirm=true. |
| `sezzle_get_merchant_context` | Merchant operations | `read` | read | No | Yes | Return non-secret environment, merchant, safety, and permission context for this server process. |
| `sezzle_get_order` | Merchant operations | `read` | read | No | Yes | Read a documented Sezzle v2 order and return a minimized financial projection without customer PII. |
| `sezzle_get_payment_session` | Merchant operations | `read` | read | No | Yes | Read a documented Sezzle v2 payment session by UUID. |
| `sezzle_preview_capture` | Merchant operations | `finance` | read | No | Yes | Read current authorization state and deterministically preview a capture without moving funds. |
| `sezzle_preview_refund` | Merchant operations | `finance` | read | No | Yes | Read captures and prior refunds, then deterministically preview a refund without moving funds. |
| `sezzle_preview_release_authorization` | Merchant operations | `finance` | read | No | Yes | Read uncaptured authorization state and preview a release without changing the order. |
| `sezzle_reauthorize_order` | Merchant operations | `finance` | write | Yes | No | Two-step reauthorization for an expired authorization. HTTP success is not treated as approval unless authorization.approved is true. |
| `sezzle_refund_order` | Merchant operations | `finance` | write | Yes | No | Execute exactly the matching refund preview. Requires preview_id and literal confirm=true. |
| `sezzle_release_authorization` | Merchant operations | `finance` | write | Yes | No | Execute exactly the matching authorization-release preview. Requires preview_id and literal confirm=true. |
| `sezzle_update_order_reference` | Merchant operations | `finance` | write | Yes | No | Two-step update of merchant tracking metadata. Call with confirm=false, then repeat unchanged with preview_id and confirm=true. |
| `sezzle_detect_capture_mismatches` | Reconciliation | `finance` | read | No | Yes | Compare merchant and Sezzle capture totals using integer minor units. |
| `sezzle_detect_fee_anomalies` | Reconciliation | `finance` | read | No | Yes | Compare expected fees with net Sezzle fees and returned fees. |
| `sezzle_detect_refund_mismatches` | Reconciliation | `finance` | read | No | Yes | Compare merchant and Sezzle refund totals using integer minor units. |
| `sezzle_explain_payout_difference` | Reconciliation | `finance` | read | No | Yes | Explain actual versus expected payout from traceable deterministic evidence; no LLM arithmetic is used. |
| `sezzle_find_unmatched_orders` | Reconciliation | `finance` | read | No | Yes | Find merchant and Sezzle order records without a deterministic match. |
| `sezzle_generate_finance_daily_brief` | Reconciliation | `finance` | read | No | Yes | Generate a deterministic finance brief of payout balance, unmatched records, duplicates, and high-priority mismatches. |
| `sezzle_reconcile_settlement` | Reconciliation | `finance` | read | No | Yes | Deterministically reconcile structured merchant orders with Sezzle settlement records and payout evidence. |
| `sezzle_get_interest_activity` | Settlement and reports | `read` | read | No | Yes | Get and parse interest-account activity at four-decimal scale. Enrollment is required by Sezzle. |
| `sezzle_get_interest_balance` | Settlement and reports | `read` | read | No | Yes | Get interest-account balance losslessly at four-decimal scale. Enrollment is required by Sezzle. |
| `sezzle_get_order_report` | Settlement and reports | `read` | read | No | Yes | Get the allowlisted Sezzle order report for a maximum seven-day range, excluding customer PII. |
| `sezzle_get_settlement_details` | Settlement and reports | `read` | read | No | Yes | Get and parse a documented settlement detail CSV by payout UUID. |
| `sezzle_list_settlement_summaries` | Settlement and reports | `read` | read | No | Yes | List documented settlement payout summaries with lossless minor-unit amounts. |
| `sezzle_classify_support_request` | Support | `support` | read | No | Yes | Classify a support request without accessing an order or selecting a route. |
| `sezzle_determine_safe_support_route` | Support | `support` | read | No | Yes | Return allowed next steps and whether a human is required for a support request. |
| `sezzle_draft_customer_response` | Support | `support` | read | No | Yes | Draft policy-safe wording. An action is called confirmed only when structured evidence includes a confirmed status and API request ID. |
| `sezzle_explain_order_status_for_support` | Support | `support` | read | No | Yes | Explain a PII-free order status only after the authenticated Sezzle order reference matches the supplied merchant reference. |
| `sezzle_identify_required_escalation` | Support | `support` | read | No | Yes | Identify required human escalation for financial, dispute, privacy, decline, or ambiguous support requests. |
| `sezzle_create_webhook` | Webhooks | `webhooks` | write | Yes | No | Two-step creation of a documented Sezzle webhook subscription. Call with confirm=false first. |
| `sezzle_delete_webhook` | Webhooks | `webhooks` | write | Yes | No | Two-step irreversible deletion of a webhook subscription. |
| `sezzle_detect_duplicate_webhook_events` | Webhooks | `webhooks` | read | No | Yes | List verified webhook events that were delivered more than once. |
| `sezzle_detect_out_of_order_events` | Webhooks | `webhooks` | read | No | Yes | Detect correlations where receipt order differs from event occurrence order; no derived order state is mutated. |
| `sezzle_find_missing_order_events` | Webhooks | `webhooks` | read | No | Yes | Compare expected webhook event types with verified events observed for each order. |
| `sezzle_get_webhook_event` | Webhooks | `webhooks` | read | No | Yes | Get verified webhook event metadata by event UUID without raw payload. |
| `sezzle_ingest_webhook_event` | Webhooks | `webhooks` | write | Yes | No | Verify then idempotently store a raw webhook event. Requires literal confirm=true; invalid signatures are rejected before parsing. |
| `sezzle_inspect_webhook_health` | Webhooks | `webhooks` | read | No | Yes | Inspect verified event, invalid-signature, duplicate-delivery, and correlation health. |
| `sezzle_list_webhook_events` | Webhooks | `webhooks` | read | No | Yes | List verified stored webhook event metadata without raw payload or customer data. |
| `sezzle_list_webhooks` | Webhooks | `webhooks` | read | No | Yes | List documented Sezzle v2 webhook subscriptions. |
| `sezzle_send_test_webhook` | Webhooks | `webhooks` | write | Yes | No | Two-step request to send a documented Sezzle test webhook to a specified URL. |
| `sezzle_update_webhook` | Webhooks | `webhooks` | write | Yes | No | Two-step replacement of a webhook URL and complete event set. Call with confirm=false first. |
| `sezzle_verify_webhook_signature` | Webhooks | `webhooks` | read | No | Yes | Verify a Sezzle HMAC-SHA256 signature against the exact raw body without parsing or storing it. |
